import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        backgroundColor: "#A19AC8",
        padding: 4,
        marginRight: 8,
        borderRadius: 4,
    },
    text: {
        fontSize: 12,
        color: "#fff",
    },
});

export default styles;